---
description: Install the Boat House CLI and connect this agent to your account.
---

Use the Boat House skill at `${CLAUDE_PLUGIN_ROOT}/skills/boathouse/SKILL.md` to install and connect. Inspect the included install script before running it with the environment's required permissions. If the CLI is already present, check `bh whoami` first. The user signs in and copies the connection message from https://boathousecloud.com/account; never request passwords or raw API keys. Once connected, return to the user's app task. Setup alone must not deploy, purchase a domain or charge a card.
